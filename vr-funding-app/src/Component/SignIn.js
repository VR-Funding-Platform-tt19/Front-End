import React, {useState, useEffect} from 'react'

// ----- Form Schema ------
import { signInFormSchema } from './FormSchemas/signInFormSchema'

// ---- Initial State -------
const initialState = {}

const SignIn = () => {

    // ----- component state --------
    const [] = useState(initialState)
    
    return (
        <div>
            {/* build out a form for logging into the app
                - username
                - password
                 */}
        </div>
    )
}

export default SignIn